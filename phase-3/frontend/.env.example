# Phase 3 Frontend Environment Variables
# Copy this file to .env.local for local development

# ===========================================
# PUBLIC VARIABLES (safe to commit)
# ===========================================

# Backend API URL
# Local: http://localhost:8000
# Production: https://amnaaplus-todo-backend.hf.space
NEXT_PUBLIC_API_URL=http://localhost:8000

# Frontend App URL (for auth redirects)
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# SECRET VARIABLES (set in Vercel dashboard, never commit!)
# ===========================================

# PostgreSQL Database URL (Neon)
# Format: postgresql://user:password@host/database?sslmode=require
# DATABASE_URL=

# Better Auth Secret (must match backend)
# Generate with: openssl rand -base64 32
# BETTER_AUTH_SECRET=
